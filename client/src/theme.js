
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#F55951', // red
    },
    secondary: {
      main: '#F1E8E6', // light pink
    },
    background: {
      paper: '#40283C', // dark purple
    },
  },
});

export default theme;
